# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running CadSense (for example via API key or ChatGPT auth supported by Codex).
- CadSense starts the server via `codex app-server` per session.
